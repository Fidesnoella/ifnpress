import {
    fetchArticles,
    setArticle,
} from './articles'
import { setSelectedAuthor, fetchPublishers } from './publisher'
import{setSelectedArticle} from'./news'

export default {
    fetchArticles,
    setArticle,
    setSelectedAuthor,
    fetchPublishers,
    setSelectedArticle,
}